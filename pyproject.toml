[project]
name = "docker-disk-doctor"
version = "0.1.0"
description = "Explain-first Docker disk usage inspector"
authors = [{name = "Andreas Revdal"}]
license = {text = "MIT"}
dependencies = [
  "docker",
  "rich"
]

[project.scripts]
docker-disk-doctor = "doctor.cli:main"


